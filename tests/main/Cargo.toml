[package]
name = "main"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
crossdylib = { path = "../../crossdylib" }
libloading = "0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"